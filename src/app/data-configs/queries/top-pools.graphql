{
  pools(first: 10, orderBy: totalValueLockedUSD, orderDirection: desc) {
    id
    volumeUSD
    totalValueLockedUSD
    observationIndex
    liquidity
    createdAtBlockNumber
    feeTier
    token0 {
      decimals
      id
      symbol
    }
    token1 {
      id
      decimals
      symbol
    }
  }
}